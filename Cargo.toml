[workspace]
members = ["pdf_extractor", "pdf_extractor_capi", "test_pdf_generator"]
resolver = "2"

[profile.dev]
opt-level = 2

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
